
namespace Enumspace
{
   enum tilemapspace
   {
       gun,
       powergun,
       laser,
       ice,
       rocket,
       threerocket,
       elect
   }
    public enum selectbuilding
    {
        notselect,
        select
    }
    public enum monsterroad
    {
        use,
        notuse

    }
    public enum buildingstate
    {
        use,
        notuse

    }
}
